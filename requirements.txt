# Core dependencies
streamlit>=1.28.0
geopandas>=0.14.0
pandas>=2.2.0
numpy>=1.26.0
rasterio>=1.3.0
sqlalchemy>=2.0.0
scipy>=1.11.0

# Visualization
matplotlib>=3.7.0
folium>=0.14.0
pydeck>=0.9.0

# Geospatial processing
shapely>=2.0.0
pyproj>=3.7.0
pylusat>=0.3.0

# LLM integration
google-generativeai>=0.3.0

# Geocoding
geopy>=2.4.0

# PDF generation
reportlab>=4.0.0
Pillow>=9.5.0

# Standard library dependencies (usually auto-installed)
requests>=2.32.0
python-dateutil>=2.9.0

# Windows-specific (comment out if not on Windows)
# GDAL installation varies by system - use conda or system package manager
# pywin32>=310  # Windows only
